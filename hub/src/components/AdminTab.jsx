// Placeholder — built out on feature/hub-admin-tab (see
// docs/BRANCH_BRIEF-hub-admin-tab.md). Ports admin.html's search ->
// quick-link -> contact -> cards flow into React, using the shared
// config from ../lib/config.js.
export function AdminTab() {
  return (
    <div className="wrap">
      <div className="card">
        <div className="step">Admin</div>
        <h2>Coming soon</h2>
        <p className="sub">
          Built on <code>feature/hub-admin-tab</code> — see
          docs/BRANCH_BRIEF-hub-admin-tab.md.
        </p>
      </div>
    </div>
  );
}
