// Placeholder — built out on feature/hub-clients-tab (see
// docs/BRANCH_BRIEF-hub-clients-tab.md). New work: every customer,
// reviews-gained snapshot, and a red/yellow/green health flag computed
// from review_snapshots the same way dashboard-app/src/Dashboard.jsx's
// snapshotAsOf() already does.
export function ClientsTab() {
  return (
    <div className="wrap">
      <div className="card">
        <div className="step">Clients</div>
        <h2>Coming soon</h2>
        <p className="sub">
          Built on <code>feature/hub-clients-tab</code> — see
          docs/BRANCH_BRIEF-hub-clients-tab.md.
        </p>
      </div>
    </div>
  );
}
